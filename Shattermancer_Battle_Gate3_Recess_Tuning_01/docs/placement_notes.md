# Placement Notes

- Draw `rune.png` at 62 × 64 pixels, repeated six times in the existing locked element-control positions.
- Draw `potionSlot.png` at 35 × 32 pixels, repeated three times on each side of CAST.
- Draw `spell.png` at 90 × 97 pixels, repeated four times in the existing locked spell-card positions.
- The opaque recesses are part of the controls and must remain intact during implementation.
- Continue supplying all element marks, names, mana state, potion icons, spell illustrations, spell names, and costs at runtime.
- No placement, scale, slot, or frame geometry changed.
